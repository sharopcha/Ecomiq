# typeorm

This library was generated with [Nx](https://nx.dev).

Shared TypeORM building blocks used by every backend service: `BaseEntity` (ULID PK), `TimestampedEntity`/`SoftDeletableEntity`, the money column transformer, tenant-scoping helpers, the `OutboxMessage` entity, and cursor pagination.
