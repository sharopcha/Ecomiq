// Placeholder kept from the original Nx-scaffolded `lib/auth.ts` — real
// exports live in roles.ts / jwt-payload.interface.ts / auth-shared.module.ts
// and are re-exported directly from src/index.ts.
export function auth(): string {
  return 'auth';
}
