// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.1
// source: marketing/v1/discount.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { handleUnaryCall, Metadata, UntypedServiceImplementation } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";

export enum DiscountKind {
  DISCOUNT_KIND_UNSPECIFIED = 0,
  PERCENTAGE = 1,
  FIXED_AMOUNT = 2,
  FREE_SHIPPING = 3,
  UNRECOGNIZED = -1,
}

/**
 * Mirrors validate-discount.util.ts's ValidateDiscountReason union exactly
 * — this contract and that util's return type must stay in lockstep (the
 * gRPC controller is a thin translation layer, see its own doc comment).
 */
export enum DiscountValidationFailureReason {
  DISCOUNT_VALIDATION_FAILURE_REASON_UNSPECIFIED = 0,
  NOT_FOUND = 1,
  INACTIVE = 2,
  NOT_STARTED = 3,
  EXPIRED = 4,
  USAGE_LIMIT_REACHED = 5,
  ONCE_PER_CUSTOMER = 6,
  MIN_SUBTOTAL_NOT_MET = 7,
  UNRECOGNIZED = -1,
}

export interface ValidateDiscountRequest {
  storeId: string;
  code: string;
  customerId?: string | undefined;
  subtotalMinor: number;
  currency: string;
}

export interface ValidateDiscountResponse {
  valid?: DiscountValidated | undefined;
  failure?: DiscountValidationFailure | undefined;
}

export interface DiscountValidated {
  discountId: string;
  discountMinor: number;
  kind: DiscountKind;
}

export interface DiscountValidationFailure {
  reason: DiscountValidationFailureReason;
  message: string;
}

function createBaseValidateDiscountRequest(): ValidateDiscountRequest {
  return { storeId: "", code: "", subtotalMinor: 0, currency: "" };
}

export const ValidateDiscountRequest: MessageFns<ValidateDiscountRequest> = {
  encode(message: ValidateDiscountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.storeId !== "") {
      writer.uint32(10).string(message.storeId);
    }
    if (message.code !== "") {
      writer.uint32(18).string(message.code);
    }
    if (message.customerId !== undefined) {
      writer.uint32(26).string(message.customerId);
    }
    if (message.subtotalMinor !== 0) {
      writer.uint32(32).int64(message.subtotalMinor);
    }
    if (message.currency !== "") {
      writer.uint32(42).string(message.currency);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidateDiscountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValidateDiscountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.customerId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.subtotalMinor = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.currency = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseValidateDiscountResponse(): ValidateDiscountResponse {
  return {};
}

export const ValidateDiscountResponse: MessageFns<ValidateDiscountResponse> = {
  encode(message: ValidateDiscountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.valid !== undefined) {
      DiscountValidated.encode(message.valid, writer.uint32(10).fork()).join();
    }
    if (message.failure !== undefined) {
      DiscountValidationFailure.encode(message.failure, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidateDiscountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValidateDiscountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.valid = DiscountValidated.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.failure = DiscountValidationFailure.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseDiscountValidated(): DiscountValidated {
  return { discountId: "", discountMinor: 0, kind: 0 };
}

export const DiscountValidated: MessageFns<DiscountValidated> = {
  encode(message: DiscountValidated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.discountId !== "") {
      writer.uint32(10).string(message.discountId);
    }
    if (message.discountMinor !== 0) {
      writer.uint32(16).int64(message.discountMinor);
    }
    if (message.kind !== 0) {
      writer.uint32(24).int32(message.kind);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscountValidated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscountValidated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.discountId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.discountMinor = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseDiscountValidationFailure(): DiscountValidationFailure {
  return { reason: 0, message: "" };
}

export const DiscountValidationFailure: MessageFns<DiscountValidationFailure> = {
  encode(message: DiscountValidationFailure, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== 0) {
      writer.uint32(8).int32(message.reason);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscountValidationFailure {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscountValidationFailure();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

export interface DiscountServiceClient {
  /**
   * Read-only — must NOT record usage (usage is recorded on
   * orders.order.placed; a validate-then-abandon checkout must
   * not burn the code). See ValidateDiscountResponse's doc comment.
   */

  validateDiscount(request: ValidateDiscountRequest, metadata?: Metadata): Observable<ValidateDiscountResponse>;
}

export interface DiscountServiceController {
  /**
   * Read-only — must NOT record usage (usage is recorded on
   * orders.order.placed; a validate-then-abandon checkout must
   * not burn the code). See ValidateDiscountResponse's doc comment.
   */

  validateDiscount(
    request: ValidateDiscountRequest,
    metadata?: Metadata,
  ): Promise<ValidateDiscountResponse> | Observable<ValidateDiscountResponse> | ValidateDiscountResponse;
}

export function DiscountServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["validateDiscount"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("DiscountService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("DiscountService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const DISCOUNT_SERVICE_NAME = "DiscountService";

export type DiscountServiceService = typeof DiscountServiceService;
export const DiscountServiceService = {
  /**
   * Read-only — must NOT record usage (usage is recorded on
   * orders.order.placed; a validate-then-abandon checkout must
   * not burn the code). See ValidateDiscountResponse's doc comment.
   */
  validateDiscount: {
    path: "/ecomiq.marketing.v1.DiscountService/ValidateDiscount" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ValidateDiscountRequest): Buffer =>
      Buffer.from(ValidateDiscountRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ValidateDiscountRequest => ValidateDiscountRequest.decode(value),
    responseSerialize: (value: ValidateDiscountResponse): Buffer =>
      Buffer.from(ValidateDiscountResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ValidateDiscountResponse => ValidateDiscountResponse.decode(value),
  },
} as const;

export interface DiscountServiceServer extends UntypedServiceImplementation {
  /**
   * Read-only — must NOT record usage (usage is recorded on
   * orders.order.placed; a validate-then-abandon checkout must
   * not burn the code). See ValidateDiscountResponse's doc comment.
   */
  validateDiscount: handleUnaryCall<ValidateDiscountRequest, ValidateDiscountResponse>;
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
