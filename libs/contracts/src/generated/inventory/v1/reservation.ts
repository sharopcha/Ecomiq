// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.1
// source: inventory/v1/reservation.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { handleUnaryCall, Metadata, UntypedServiceImplementation } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { wrappers } from "protobufjs";
import { Observable } from "rxjs";
import { Timestamp } from "../../google/protobuf/timestamp";

export enum ReservationFailureReason {
  RESERVATION_FAILURE_REASON_UNSPECIFIED = 0,
  INSUFFICIENT_STOCK = 1,
  VARIANT_NOT_FOUND = 2,
  LOCATION_NOT_FOUND = 3,
  UNRECOGNIZED = -1,
}

export interface ReserveStockRequest {
  storeId: string;
  variantId: string;
  /**
   * Omitted = let inventory-service pick a location for this variant.
   * Set explicitly when the
   * caller already knows which warehouse should fulfill the line.
   */
  locationId?: string | undefined;
  qty: number;
  orderId: string;
  orderLineId: string;
  /**
   * Required. Retrying the same saga step (timeout, redelivery) must not
   * double-reserve — keying on this makes ReserveStock safe to retry.
   */
  idempotencyKey: string;
}

export interface ReserveStockResponse {
  reserved?: ReservationReserved | undefined;
  failure?: ReservationFailure | undefined;
}

export interface ReservationReserved {
  reservationId: string;
  reservedUntil: Date | undefined;
}

export interface ReservationFailure {
  reason: ReservationFailureReason;
  message: string;
}

export interface ReleaseReservationRequest {
  storeId: string;
  reservationId: string;
  /**
   * Same retry-safety purpose as ReserveStockRequest's — releasing twice
   * must not double-decrement `reserved` (ReservationsService.release()
   * already refuses an already-released reservation; this lets a retried
   * call observe that as "already done" rather than a saga-visible error).
   */
  idempotencyKey: string;
}

export interface ReleaseReservationResponse {
  released?: ReservationReleased | undefined;
  failure?: ReservationFailure | undefined;
}

export interface ReservationReleased {
  reservationId: string;
  releasedAt: Date | undefined;
}

wrappers[".google.protobuf.Timestamp"] = {
  fromObject(value: Date) {
    return { seconds: value.getTime() / 1000, nanos: (value.getTime() % 1000) * 1e6 };
  },
  toObject(message: { seconds: number; nanos: number }) {
    return new Date(message.seconds * 1000 + message.nanos / 1e6);
  },
} as any;

function createBaseReserveStockRequest(): ReserveStockRequest {
  return { storeId: "", variantId: "", qty: 0, orderId: "", orderLineId: "", idempotencyKey: "" };
}

export const ReserveStockRequest: MessageFns<ReserveStockRequest> = {
  encode(message: ReserveStockRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.storeId !== "") {
      writer.uint32(10).string(message.storeId);
    }
    if (message.variantId !== "") {
      writer.uint32(18).string(message.variantId);
    }
    if (message.locationId !== undefined) {
      writer.uint32(26).string(message.locationId);
    }
    if (message.qty !== 0) {
      writer.uint32(32).int32(message.qty);
    }
    if (message.orderId !== "") {
      writer.uint32(42).string(message.orderId);
    }
    if (message.orderLineId !== "") {
      writer.uint32(50).string(message.orderLineId);
    }
    if (message.idempotencyKey !== "") {
      writer.uint32(58).string(message.idempotencyKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReserveStockRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReserveStockRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.variantId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.locationId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.qty = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.orderId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.orderLineId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.idempotencyKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseReserveStockResponse(): ReserveStockResponse {
  return {};
}

export const ReserveStockResponse: MessageFns<ReserveStockResponse> = {
  encode(message: ReserveStockResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reserved !== undefined) {
      ReservationReserved.encode(message.reserved, writer.uint32(10).fork()).join();
    }
    if (message.failure !== undefined) {
      ReservationFailure.encode(message.failure, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReserveStockResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReserveStockResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reserved = ReservationReserved.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.failure = ReservationFailure.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseReservationReserved(): ReservationReserved {
  return { reservationId: "", reservedUntil: undefined };
}

export const ReservationReserved: MessageFns<ReservationReserved> = {
  encode(message: ReservationReserved, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reservationId !== "") {
      writer.uint32(10).string(message.reservationId);
    }
    if (message.reservedUntil !== undefined) {
      Timestamp.encode(toTimestamp(message.reservedUntil), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReservationReserved {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReservationReserved();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reservedUntil = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseReservationFailure(): ReservationFailure {
  return { reason: 0, message: "" };
}

export const ReservationFailure: MessageFns<ReservationFailure> = {
  encode(message: ReservationFailure, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== 0) {
      writer.uint32(8).int32(message.reason);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReservationFailure {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReservationFailure();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseReleaseReservationRequest(): ReleaseReservationRequest {
  return { storeId: "", reservationId: "", idempotencyKey: "" };
}

export const ReleaseReservationRequest: MessageFns<ReleaseReservationRequest> = {
  encode(message: ReleaseReservationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.storeId !== "") {
      writer.uint32(10).string(message.storeId);
    }
    if (message.reservationId !== "") {
      writer.uint32(18).string(message.reservationId);
    }
    if (message.idempotencyKey !== "") {
      writer.uint32(26).string(message.idempotencyKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReleaseReservationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReleaseReservationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reservationId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.idempotencyKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseReleaseReservationResponse(): ReleaseReservationResponse {
  return {};
}

export const ReleaseReservationResponse: MessageFns<ReleaseReservationResponse> = {
  encode(message: ReleaseReservationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.released !== undefined) {
      ReservationReleased.encode(message.released, writer.uint32(10).fork()).join();
    }
    if (message.failure !== undefined) {
      ReservationFailure.encode(message.failure, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReleaseReservationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReleaseReservationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.released = ReservationReleased.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.failure = ReservationFailure.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseReservationReleased(): ReservationReleased {
  return { reservationId: "", releasedAt: undefined };
}

export const ReservationReleased: MessageFns<ReservationReleased> = {
  encode(message: ReservationReleased, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reservationId !== "") {
      writer.uint32(10).string(message.reservationId);
    }
    if (message.releasedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.releasedAt), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReservationReleased {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReservationReleased();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.releasedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

/**
 * Backs order-service's checkout saga (ADR-7): a synchronous call per order
 * line to hold stock before payment, and its compensating action if a later
 * saga step fails. The server side lives in inventory-service
 * (reservation-grpc.controller.ts), translating these messages onto
 * the existing ReservationsService.create()/release() — all
 * locking/negative-guard/outbox logic already lives there, this contract
 * doesn't change any of it.
 */

export interface ReservationServiceClient {
  reserveStock(request: ReserveStockRequest, metadata?: Metadata): Observable<ReserveStockResponse>;

  releaseReservation(request: ReleaseReservationRequest, metadata?: Metadata): Observable<ReleaseReservationResponse>;
}

/**
 * Backs order-service's checkout saga (ADR-7): a synchronous call per order
 * line to hold stock before payment, and its compensating action if a later
 * saga step fails. The server side lives in inventory-service
 * (reservation-grpc.controller.ts), translating these messages onto
 * the existing ReservationsService.create()/release() — all
 * locking/negative-guard/outbox logic already lives there, this contract
 * doesn't change any of it.
 */

export interface ReservationServiceController {
  reserveStock(
    request: ReserveStockRequest,
    metadata?: Metadata,
  ): Promise<ReserveStockResponse> | Observable<ReserveStockResponse> | ReserveStockResponse;

  releaseReservation(
    request: ReleaseReservationRequest,
    metadata?: Metadata,
  ): Promise<ReleaseReservationResponse> | Observable<ReleaseReservationResponse> | ReleaseReservationResponse;
}

export function ReservationServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["reserveStock", "releaseReservation"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("ReservationService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("ReservationService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const RESERVATION_SERVICE_NAME = "ReservationService";

/**
 * Backs order-service's checkout saga (ADR-7): a synchronous call per order
 * line to hold stock before payment, and its compensating action if a later
 * saga step fails. The server side lives in inventory-service
 * (reservation-grpc.controller.ts), translating these messages onto
 * the existing ReservationsService.create()/release() — all
 * locking/negative-guard/outbox logic already lives there, this contract
 * doesn't change any of it.
 */
export type ReservationServiceService = typeof ReservationServiceService;
export const ReservationServiceService = {
  reserveStock: {
    path: "/ecomiq.inventory.v1.ReservationService/ReserveStock" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ReserveStockRequest): Buffer => Buffer.from(ReserveStockRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ReserveStockRequest => ReserveStockRequest.decode(value),
    responseSerialize: (value: ReserveStockResponse): Buffer =>
      Buffer.from(ReserveStockResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ReserveStockResponse => ReserveStockResponse.decode(value),
  },
  releaseReservation: {
    path: "/ecomiq.inventory.v1.ReservationService/ReleaseReservation" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ReleaseReservationRequest): Buffer =>
      Buffer.from(ReleaseReservationRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ReleaseReservationRequest => ReleaseReservationRequest.decode(value),
    responseSerialize: (value: ReleaseReservationResponse): Buffer =>
      Buffer.from(ReleaseReservationResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ReleaseReservationResponse => ReleaseReservationResponse.decode(value),
  },
} as const;

export interface ReservationServiceServer extends UntypedServiceImplementation {
  reserveStock: handleUnaryCall<ReserveStockRequest, ReserveStockResponse>;
  releaseReservation: handleUnaryCall<ReleaseReservationRequest, ReleaseReservationResponse>;
}

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
